# Validation — 41.90-public.1

Local deterministic suite: **94/94 tests passed**, Python 3.13.5 on Linux, September 24, 2026. Tests exercise the public edition only; no private engine was run. The final archive is subjected to the same suite and exact manifest verification after fresh extraction; final artifact digests and outcomes are recorded outside this self-referential payload.

## Acceptance scope

Critical: fixed public quantity/target arithmetic; scoped evidence and freshness; strict synthetic input and fee-unit schemas; deterministic duplicates; ambiguous state; no runtime transport or credential capability; exact identity before planner import; hash/extra/missing-file and invalid-control rejection. Cost-backed tests check full-packet costs, both fee models at every tick, stale depth, passive permission, partial-fill refusal and no implied fill.

High: standalone export with main entrypoint, planner and metadata removed; tampered support never executed through the main bootstrap; same-process Critical deduplication; same-machine contention; privacy and bounded failure; low-storage fixture; output-link refusal; symlink input refusal; no final ZIP visible during compression; capsule-only outcome; unrelated working directory; spaces and shell characters in a path; harmless menu EOF and invalid-input non-trigger.

Package: 29 regular files, 28 managed payload hashes plus the manifest; exactly one main BAT and one independent Export BAT. Source parses without generating cache bytecode. Existing license and third-party notice bytes are preserved. Exact supported baseline Git-tree hashes are retained in the external receipt.

## Evidence limits

GitHub-hosted Ubuntu/Windows CI runs Python 3.11 and 3.13; its results must be read for the exact published commit, not inherited. Windows symlink creation can be unavailable and is reported as a skip rather than a pass. Existing legacy tests use OS-managed temporary directories; application diagnostics remain project-local.

Native user-PC/Norton testing, signing, real exchange behavior, profitability, exhaustive history scanning, and privileged filesystem race guarantees are NOT RUN or NOT CLAIMED. The two-second cooperative export budget cannot forcibly interrupt a blocked OS filesystem call. Missing Python reports unavailable capture; no runtime installation or unsafe fallback is attempted.

Copyright © 2026 Gateway Information Group LLC. All rights reserved.
